name: Deletion of release versions
on:
  push:
  workflow_dispatch:

env:
    GH_TOKEN: ${{ github.token }}  

jobs:
  deletion-of-tags:
    runs-on: ubuntu-latest
    name: releases
    
    steps:
    - name: checkout
      uses: actions/checkout/@v3
         
   
    - name: delete
      run: |
          gh release delete GENX_UAT_SP49_05Sep23/uk1.4
          --cleanup-tag
          -y
          
